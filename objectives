-- initialize
os.loadAPI("log")
log.init(2)
log.debug("Initializing")

os.loadAPI("ae2")
ae2.init("left")

os.loadAPI("scripts")

os.loadAPI("screen")
screen.init("monitor_3")

os.loadAPI("utils")

-- body
--scripts.loadLevel("easy")
local pane = {5,5,20,20}
paintutils.drawBox(4,4,21,21,colors.blue)
term.setBackgroundColor(colors.black)
screen.writeInPane("abc def ghi jkl mno pqr stu",pane,8,12)

-- cleanup
log.close()
screen.close()